{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PagesCarrousel</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Number</th>


                {% for t  in entity.caroussels %}
                    {{ dump(t)}}
                    {{ t.fleches}}
                    {#{% for s in t.caroussels %}#}
                        {#{{ s.fleches}}#}
                    {#{% endfor %}#}

                {% endfor %}
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pagescarrousel') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pagescarrousel_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
